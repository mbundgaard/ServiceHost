<Window x:Class="ServiceHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ServiceHost"
        xmlns:vm="clr-namespace:ServiceHost.ViewModels"
        xmlns:models="clr-namespace:ServiceHost.Models"
        Title="ServiceHost"
        Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Start All"
                            Command="{Binding StartAllCommand}"
                            Style="{StaticResource ActionButtonStyle}"
                            Margin="0,0,8,0"/>
                    <Button Content="Stop All"
                            Command="{Binding StopAllCommand}"
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#757575"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="API: :" Foreground="#666"/>
                    <TextBlock Text="{Binding ApiPort}" Foreground="#666" Margin="0,0,4,0"/>
                    <Ellipse Width="8" Height="8"
                             Fill="{StaticResource SuccessBrush}"
                             VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- Service List -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Services"
                           FontWeight="SemiBold"
                           Padding="12,8"
                           Background="#F5F5F5"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Services}"
                         SelectedItem="{Binding SelectedService}"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="8,6"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Status Indicator -->
                                <Ellipse Grid.Column="0"
                                         Width="10" Height="10"
                                         Margin="0,0,8,0"
                                         VerticalAlignment="Center"
                                         Fill="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>

                                <!-- Service Name -->
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <Button Command="{Binding StartCommand}"
                                            Style="{StaticResource IconButtonStyle}"
                                            ToolTip="Start"
                                            IsEnabled="{Binding IsOperating, Converter={StaticResource InverseBoolConverter}}">
                                        <TextBlock Text="▶" FontSize="10" Foreground="{StaticResource SuccessBrush}"/>
                                    </Button>
                                    <Button Command="{Binding StopCommand}"
                                            Style="{StaticResource IconButtonStyle}"
                                            ToolTip="Stop"
                                            IsEnabled="{Binding IsOperating, Converter={StaticResource InverseBoolConverter}}">
                                        <TextBlock Text="■" FontSize="10" Foreground="{StaticResource DangerBrush}"/>
                                    </Button>
                                    <Button Command="{Binding RestartCommand}"
                                            Style="{StaticResource IconButtonStyle}"
                                            ToolTip="Restart"
                                            IsEnabled="{Binding IsOperating, Converter={StaticResource InverseBoolConverter}}">
                                        <TextBlock Text="↻" FontSize="12" Foreground="{StaticResource PrimaryBrush}"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="4"
                          Background="#E0E0E0"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"/>

            <!-- Log Viewer -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Padding="12,8"
                           Background="#F5F5F5">
                    <Run Text="Logs: " FontWeight="SemiBold"/>
                    <Run Text="{Binding SelectedService.Name, Mode=OneWay, FallbackValue='(select a service)'}"/>
                </TextBlock>

                <TextBox Grid.Row="1"
                         Text="{Binding LogContent, Mode=OneWay}"
                         IsReadOnly="True"
                         FontFamily="Consolas"
                         FontSize="12"
                         Background="#1E1E1E"
                         Foreground="#D4D4D4"
                         BorderThickness="0"
                         Padding="8"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         x:Name="LogTextBox"/>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="8,4">
            <TextBlock Text="{Binding StatusText}" Foreground="#666"/>
        </Border>
    </Grid>
</Window>
